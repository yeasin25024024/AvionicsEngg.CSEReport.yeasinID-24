#include <stdio.h>
int main() 
{
    int n;
    printf("Enter an integer: ");
    scanf("%d", &n);

    if(num % 2 == 0)
        printf("%d is even.", num);
    else
        printf("%d is odd.", num);
    
    return 0;
}
